{% extends 'base.html.twig' %}

{% block title %}Tehran Tour - Attractions{% endblock %}

{% block body %}
    {% if attractions is empty %}
        <h2>There is no Attraction</h2>
    {% else %}
        <ul>
            {% for attraction in attractions %}
                <li>
                    <a href="{{ path('app_attraction_view', {id: attraction.id}) }}">{{ attraction.name }} ({{ attraction.score }})</a>
                    <a href="{{ path('app_attraction_edit', {id: attraction.id}) }}">edit</a>
                    <a href="{{ path('app_attraction_delete', {id: attraction.id}) }}">delete</a>
                </li>
            {% endfor %}
        </ul>
    {% endif %}

    <a href="{{ path('app_attraction_new') }}">Create New Attraction</a>
{% endblock %}
